package pages

import "gen-img/internal/templs/layouts"
import "gen-img/internal/templs/components"


templ Home() {
    @layouts.BaseLayout("Generate Images") {

        <header class="flex flex-col justify-center h-[4rem] p-8">
            // Title 
            <p class="font-bold text-2xl">🦫 gen-img</p>

        </header>

        <div class="flex flex-row h-[calc(100vh-4rem)] justify-between px-8 gap-8">
            
            <div class="flex flex-row gap-2">
                @components.RequestForm()

                // Divider
                <div class="divider divider-horizontal">👉</div>
            </div>
            

            <div id="image-list-container" class="overflow-y-auto h-full">
            </div>
        </div>
    }
   
}